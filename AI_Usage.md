# AI usage log

Use this file for every submission. Follow this structure:

1. Prompts I used
2. What the AI suggested (summary)
3. What I changed and why
4. What I tested / verified
5. What I learned

I used AI to help make find the unnormalizations in the ddl file I had for ia510-mp1.
I just asked it to normalize the ddl schema from the attached file.  I knew there wouldn't many updates and it
would all come from ORDERS and ORDER_LINE.
I pasted the new DDL into after.qsl file. I started the note header for the queries for TotalAmount and LineTotal
and it already knew what my next lines were going to be for the querey.  So I didn't use an AI model like Copilot for help
with those queries.  I am assuming it was a built in AI in VS Code that knew what my next lines were going to be.
They looked correct so I kept them.

I also used AI to help with some of the errors I was getting due to duplicate creates for tables and for the TotalLine having and issue in the query. 